__pycache__/
*.pyc
.last-update-check
# Per-install secrets and audit trails — never commit (root-only patterns so
# test fixtures like tests/fixtures/test.salt aren't blocked).
/.salt
/audit.log
/.git-credentials
/settings.json
/last_run.json
